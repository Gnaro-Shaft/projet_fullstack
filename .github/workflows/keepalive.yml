name: keepalive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: |
          curl -s --max-time 30 -o /dev/null -w "%{http_code}" \
            https://projet-fullstack.fly.dev/ping
